SKIP_PREFLIGHT_CHECK=true

# The frontend talks to the backend API at /api, which Vite proxies to the
# Node/Turso server (see vite.config.ts + server/index.mjs). Backend DB
# credentials live in .env (TURSO_DATABASE_URL / TURSO_AUTH_TOKEN), not here.
VITE_IS_DEMO=false
