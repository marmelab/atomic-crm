make registry-gen

# Auto-increment version in src/version.ts
VERSION_FILE="src/version.ts"
CURRENT=$(grep -o 'v1\.[0-9]*' "$VERSION_FILE" | head -1)
NUM=$(echo "$CURRENT" | grep -o '[0-9]*$')
NEXT=$((NUM + 1))
sed -i '' "s/v1\.$NUM/v1.$NEXT/" "$VERSION_FILE"
git add "$VERSION_FILE"
