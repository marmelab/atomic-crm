# See https://help.github.com/ignore-files/ for more about ignoring files.

# editors
.idea/
*.iml
.vscode/

# dependencies
/node_modules

# testing
/coverage
__screenshots__
.vitest-attachments/

# production
/build

# misc
.DS_Store
.env.local
.env.production
.env.development.local
.env.test.local
.env.production.local
.eslintcache

npm-debug.log*
yarn-debug.log*
yarn-error.log*
supabase/.branches
supabase/.temp
.supabase-e2e/

dist/
/lib/
.env
*storybook.log

# Shadcn Registry
public/r/

!supabase/functions/.env
.claude/settings.local.json

# Harness scratch (promotion lock; worktrees + test-results also cleaned at SessionEnd)
.promote.lock
# Rendered per-session harness board (render-status hook; cleaned at SessionEnd)
/.harness/

# Playwright
.playwright-mcp/
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

*.lock

.vercel
.env*

# Historical Migration slice: real pulled source data (Notion/Acuity/Stripe
# exports, the staging manifest, real rulings) must never be committed —
# see scripts/historical-import/README.md. Only the *.example.json shape
# references and the pure planning/writing code itself are tracked.
/scripts/historical-import/data/
