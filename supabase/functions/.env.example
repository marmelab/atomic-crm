# Supabase Edge Function Secrets Reference
#
# For LOCAL DEVELOPMENT, edit supabase/functions/.env directly.
#
# For REMOTE/PRODUCTION, push secrets using:
#   npx supabase secrets set KEY=value --project-ref <project-ref>
#
# The supabase-remote-init script pushes SB_PUBLISHABLE_KEY and
# SB_SECRET_KEY automatically during initial setup.

# ============================================================
# Required (set automatically by supabase-remote-init)
# ============================================================

# Supabase publishable (anon) key - same as VITE_SB_PUBLISHABLE_KEY
SB_PUBLISHABLE_KEY=

# Supabase service role (secret) key
# Found at: Supabase Dashboard > Project Settings > API > service_role
# NEVER expose this key in frontend code
SB_SECRET_KEY=

# ============================================================
# Optional overrides
# ============================================================

# Override the JWT issuer URL (auto-derived from SUPABASE_URL if not set)
# SB_JWT_ISSUER=

# ============================================================
# Optional (for inbound email via Postmark)
# ============================================================

# Credentials for the Postmark webhook endpoint
# POSTMARK_WEBHOOK_USER=
# POSTMARK_WEBHOOK_PASSWORD=

# Comma-separated list of IPs authorized to call the webhook
# See: https://postmarkapp.com/support/article/800-ips-for-firewalls#webhooks
# POSTMARK_WEBHOOK_AUTHORIZED_IPS=3.134.147.250,50.31.156.6,50.31.156.77,18.217.206.57
