# W2 handoff

Plan: [`poc-plan.md`](poc-plan.md) §9 / §11. Click the Woodley triangle. No full roster (W3). No AWS (W4).

## Done

- BFF `GET` show payloads for contacts (types, identifiers, affiliations, links, deals), companies (parent/children/people), deals (parties + stages)
- `PATCH /deals/:id` writes `stage_id`, `deal_stage_events`, and `activities`
- Graph list/show pages wired as the default contact / company / deal resources
- Second In Process stage so a stage move is visible
- `make w2-isolation`

```bash
make w1-smoke
make start-bff
npm run dev
# open /#/contacts → Willow Woodley
```

## Not in this PR

- Atomic notes/tasks/timeline chrome
- Saved views / merge / Woodley theme
- Drag-and-drop kanban (columns + stage buttons only)
