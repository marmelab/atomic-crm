# W0 handoff

Plan: [`poc-plan.md`](poc-plan.md). Branch: `w0/local-postgres` (PR #2).

## Done in this PR

- Docker Postgres on port **5433** (`docker-compose.yml`)
- Apply [`schema-direction.sql`](schema-direction.sql) including tenant RLS
- Seed Woodley **`100004`** and Envoy **`100081`** (`sql/seed_w0.sql`)
- Deterministic tenant UUID: `uuid5(uuid5(DNS, 'ardley-crm.tenants'), customer_id)` — see `scripts/tenant_uuid.py`
- App role `crm_app` sets `app.tenant_id`; table owner/superuser bypasses RLS

```bash
make w0-smoke
```

Smoke was **not** executed in the session that opened the PR (no Docker on that PATH). Run it locally before continuing.

## Not done (rest of W0)

- Strip `@supabase/*`. `make start` is still Atomic + local Supabase.
- Cognito app client — **W4**. Locally stub the Acorn principal (`tenant_id` = customer id string).
- Empty Lambda hello / AWS — **W4**.
- Graph UI, pipelines, seed story — **W1–W3**. Do not add mortgage fields on the Atomic/Supabase schema.

## Constraints (do not reopen)

- Platform Cognito pools only (`ardley-app-users-{env}`). Ignore `ardley-customers-*.cognito_user_pool_id`.
- Acorn isolation value is the customer id (`"100004"` / `"100081"`), not `tenants.id`.
- `owner_id` = `crm_users.id`. Cognito `sub` is lookup-only.
- Principal from Acorn extractor (SSO claims / role store / `ardley-customer-users-*`). No CRM-local allow-list.
- Fake PII only.

## Next session

Repo: `~/projects/ardley-crm` (not `plaid_expense_sync`). Checkout `w0/local-postgres`. Plan is merged on `main` as PR #1 (`docs/poc-plan.md`) — do not rewrite it. `AGENTS.md` is still Atomic/Supabase; ignore `make start` for this work.

Git: short-lived PRs off `main`. AWS **dev** is an env, not a git branch. Do not create a long-lived `dev` branch. Wiki/Discussions after merge; this file + the PR body are the handoff.

Tenant UUIDs (already in `sql/seed_w0.sql`):

| customer | id | `tenants.id` |
|---|---|---|
| Woodley | `100004` | `4b51bd26-ea4f-5777-b9d9-780dbb91853e` |
| Envoy | `100081` | `28dd4130-fe59-5ada-a3ce-78c82259e9dd` |

Namespace: `uuid5(DNS, 'ardley-crm.tenants')` = `e8677963-9f25-577d-b91b-76a7882e090d`.

1. `make w0-smoke` with Docker. If it fails, fix this PR; do not merge yet.
2. If smoke is green, merge this PR as the Postgres slice, then open a new short PR (e.g. `w0/local-data-path`) for the rest of W0.
3. Replace Supabase on that next PR. Entry points:
   - `src/components/atomic-crm/providers/supabase/` (`dataProvider.ts`, `authProvider.ts`, `supabase.ts`)
   - wired from `src/components/atomic-crm/root/CRM.tsx`
   - types re-export in `src/components/atomic-crm/providers/types.ts`
   Local path: queries against Docker Postgres on **5433** (or a tiny HTTP BFF). Stub the Acorn principal (`tenant_id` = customer id string `"100004"` / `"100081"`). No Cognito, no Lambda.
4. Isolation check on that path: a Woodley session never returns Ellis Envoy.

Do not add mortgage fields, graph UI, or AWS in this wave.
