# W0 handoff

Plan: [`poc-plan.md`](poc-plan.md). Postgres slice is on `main` (PR #2). Local data path: `w0/local-data-path`.

## Done in this PR

- Docker Postgres on port **5433** (`docker-compose.yml`)
- Apply [`schema-direction.sql`](schema-direction.sql) including tenant RLS
- Seed Woodley **`100004`** and Envoy **`100081`** (`sql/seed_w0.sql`)
- Deterministic tenant UUID: `uuid5(uuid5(DNS, 'ardley-crm.tenants'), customer_id)` — see `scripts/tenant_uuid.py`
- App role `crm_app` sets `app.tenant_id`; table owner/superuser bypasses RLS

```bash
make w0-smoke
```

`make w0-smoke` passed on a machine with Docker (Woodley cannot see Envoy at SQL).

## Local data path (this PR)

- Tiny HTTP BFF: `server/local-bff.mjs` on **8787**, Postgres **5433**, role `crm_app`
- Stub principal header `x-ardley-customer-id: 100004` | `100081` (Acorn customer id string)
- App default providers: `src/components/atomic-crm/providers/local/`
- `make start` → Postgres + BFF (no Supabase)
- Isolation: `make w0-isolation` (needs BFF + seeded DB)

```bash
make w0-smoke
make start-bff   # other terminal
make w0-isolation
```

## Constraints (do not reopen)

- Platform Cognito pools only (`ardley-app-users-{env}`). Ignore `ardley-customers-*.cognito_user_pool_id`.
- Acorn isolation value is the customer id (`"100004"` / `"100081"`), not `tenants.id`.
- `owner_id` = `crm_users.id`. Cognito `sub` is lookup-only.
- Principal from Acorn extractor (SSO claims / role store / `ardley-customer-users-*`). No CRM-local allow-list.
- Fake PII only.

## Next after this PR

Repo: `~/projects/ardley-crm`. Plan stays [`poc-plan.md`](poc-plan.md) — do not rewrite it.

Git: short-lived PRs off `main`. AWS **dev** is an env, not a git branch. Do not create a long-lived `dev` branch.

Tenant UUIDs (already in `sql/seed_w0.sql`):

| customer | id | `tenants.id` |
|---|---|---|
| Woodley | `100004` | `4b51bd26-ea4f-5777-b9d9-780dbb91853e` |
| Envoy | `100081` | `28dd4130-fe59-5ada-a3ce-78c82259e9dd` |

Namespace: `uuid5(DNS, 'ardley-crm.tenants')` = `e8677963-9f25-577d-b91b-76a7882e090d`.

Still later (not this PR):

- Delete unused `@supabase/*` / `ra-supabase-*` once lists besides contacts are on the BFF
- Cognito app client and Lambda — **W4**
- Graph UI, pipelines, seed story — **W1–W3**

Do not add mortgage fields, graph UI, or AWS in this wave.
