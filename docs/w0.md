# W0 handoff

Plan: [`poc-plan.md`](poc-plan.md). Branch: `w0/local-postgres` (PR #2).

## Done in this PR

- Docker Postgres on port **5433** (`docker-compose.yml`)
- Apply [`schema-direction.sql`](schema-direction.sql) including tenant RLS
- Seed Woodley **`100004`** and Envoy **`100081`** (`sql/seed_w0.sql`)
- Deterministic tenant UUID: `uuid5(uuid5(DNS, 'ardley-crm.tenants'), customer_id)` — see `scripts/tenant_uuid.py`
- App role `crm_app` sets `app.tenant_id`; table owner/superuser bypasses RLS

```bash
make w0-smoke
```

Smoke was **not** executed in the session that opened the PR (no Docker on that PATH). Run it locally before continuing.

## Not done (rest of W0)

- Strip `@supabase/*`. `make start` is still Atomic + local Supabase.
- Cognito app client — **W4**. Locally stub the Acorn principal (`tenant_id` = customer id string).
- Empty Lambda hello / AWS — **W4**.
- Graph UI, pipelines, seed story — **W1–W3**. Do not add mortgage fields on the Atomic/Supabase schema.

## Constraints (do not reopen)

- Platform Cognito pools only (`ardley-app-users-{env}`). Ignore `ardley-customers-*.cognito_user_pool_id`.
- Acorn isolation value is the customer id (`"100004"` / `"100081"`), not `tenants.id`.
- `owner_id` = `crm_users.id`. Cognito `sub` is lookup-only.
- Principal from Acorn extractor (SSO claims / role store / `ardley-customer-users-*`). No CRM-local allow-list.
- Fake PII only.

## Next

1. `make w0-smoke` on a machine with Docker.
2. Replace the Supabase data provider with queries against this Postgres (or a tiny local HTTP BFF) without wiring AWS.
3. Keep two-tenant isolation in that path (Woodley session never returns Ellis Envoy).
