# W4 handoff

Plan: [`poc-plan.md`](poc-plan.md) §8 / §11. Public HTTPS on **dev**.

## Domains

Hosted zone `ardley.us` (`Z05525842X4KMDBJ1JLNK`). Sibling to existing `dev.ardley.us` / `api.dev.ardley.us`.

| Surface | Host |
|---|---|
| SPA | `https://crm.dev.ardley.us` |
| API | `https://api.crm.dev.ardley.us` |

Do not use `{customer}.dev-ardley.com` (CES tenant hostnames).

## This PR (skeleton)

CDK app in `infra/`:

- New VPC (2 AZs, 1 NAT)
- Aurora Serverless v2 Postgres (`ardley_crm`), secret generated, deletion protection on
- Hello Lambda + HTTP API `/health` (no Cognito / Acorn yet)
- S3 + CloudFront placeholder page
- ACM (DNS-validated) + Route53 aliases

```bash
cd infra
npm install
npx cdk synth
# deploy is a human step: npx cdk deploy ArdleyCrmDevStack
```

Account: `098330792857`, region `us-east-1`.

## Not in this PR

- Cognito app client on `ardley-app-users-dev`
- Acorn principal (drop stub header)
- Seed / isolation against Aurora
- Vite SPA pointed at the API
- `cdk deploy` (you run that)
