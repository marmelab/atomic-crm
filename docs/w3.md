# W3 handoff

Plan: [`poc-plan.md`](poc-plan.md) §10 / §11.

## Done

- Walkthrough census (`sql/seed_w3_roster.sql`, generated) on top of the hand-seeded triangles
- Saved-view home, ENV paired-agent list
- Merge: `POST /contacts/merge` sets `merged_into_id`, reassigns graph rows, does not delete people
- Woodley CRM title; Home / Opportunities nav; loan amount copy
- Coaching stub on LO / employee; dead **Issue pre-approval** on in-process deals
- `make w3-smoke` / `make w3-isolation`

```bash
make w3-smoke
make start-bff
npx vite --port 5176 --strictPort
```

## Not in this PR

- AWS (W4)
- Full Atomic notes/kanban chrome
